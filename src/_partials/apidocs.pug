.apidocs
  .pagewrap
    .sidebar
      section
        h2 
          a(href="#" + docNavLinks.connect)=docNavLinks.connect
        ul
      section
        h2 
          a(href="#" + docNavLinks.js)=docNavLinks.js
        ul
      section
        h2 
          a(href="#" + docNavLinks.registry)=docNavLinks.registry
        ul
    .content
      section.lib-section
        h1.lib-header(id=docNavLinks.connect)=docNavLinks.connect
        .lib-doc
          include:markdown-it(html) uport-connect-api.md
      section.lib-section
        h1.lib-header(id=docNavLinks.js)=docNavLinks.js
        .lib-doc
          //- DOCS 
      section.lib-section
        h1.lib-header(id=docNavLinks.registry)=docNavLinks.registry
        .lib-doc
          //- DOCS
